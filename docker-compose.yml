services:
  app:
    image: doljanchi-invitation:latest
    container_name: doljanchi-invitation
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
